---
import BaseHead from "../components/BaseHead.astro";
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
import { Image } from "astro:assets";
import headShot from "../assets/sagiri.jpg";
import PublicationCard from "../components/Publication.astro";
import publications from "../data/publications.json";
import Experience from "../components/Experience.astro";
import WorkExperience from "../components/WorkExperience.astro";
import experienceData from "../data/experience.json";
import resume from "../assets/resume.pdf";
import { Icon } from "astro-icon/components";
const sortedPubs = publications.sort((a, b) => {
	return new Date(b.pubDate).getTime() - new Date(a.pubDate).getTime();
});
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead
			title={SITE_TITLE}
			description={SITE_DESCRIPTION}
			image={headShot}
		/>

		<style>
			#container-headshot {
				width: 700px;
				height: 200px;
			}

			#head-shot {
				width: 200px;
				height: 200px;
				border-radius: 50%;
				object-fit: cover;
				box-shadow: 0 0 10px 0 rgba(0, 0, 0, 0.1);

				transition: all 0.7s cubic-bezier(0.6, -0.28, 0.735, 0.045);
			}

			#container-headshot {
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				gap: 1rem;
			}
			#head-shot:hover {
				transform: scale(1.15) rotate(360deg);
			}

			#container-intro {
				display: flex;
				flex-direction: row;
				align-items: center;
				justify-content: center;
				gap: 5rem;
			}

			#container-intro-research-list {
				margin-top: 80px;
			}

			.social-links {
				display: flex;
				gap: 1.1rem;
			}
			p {
				line-height: 1.55;
				margin: 11px 0;
			}
			ul {
				list-style: none;
				padding-left: 0;
			}

			#teaser {
				width: 70%;

				margin: auto;
				padding: 3em 1em;
			}

			/* 手机尺寸下调整布局 */
			@media (max-width: 720px) {
				#container-headshot {
					width: 100%;
					height: auto;
				}

				#head-shot {
					max-width: 150px;
				}

				#container-intro {
					flex-direction: column;
					gap: 2rem;
				}
			}

			/* Float up 动画 */
			@keyframes floatUp {
				from {
					opacity: 0;
					transform: translateY(30px);
				}
				to {
					opacity: 1;
					transform: translateY(0);
				}
			}

			#teaser {
				animation: floatUp 0.8s ease-out forwards;
			}

			/* 为子元素添加延迟动画 */
			#container-intro {
				animation: floatUp 0.8s ease-out 0.2s forwards;
				opacity: 0;
			}

			#container-intro-research-list {
				animation: floatUp 0.8s ease-out 0.4s forwards;
				opacity: 0;
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<div id="teaser">
				<div id="container-intro">
					<div id="container-headshot">
						<Image src={headShot} id="head-shot" alt="Hongyi Cai" />

						<div class="social-links">
							<a
								href="https://github.com/xcloudfance"
								target="_blank"
							>
								<Icon name="mdi:github" size={32} />
							</a>
							<a
								href="https://scholar.google.com/citations?user=nQHDIlUAAAAJ"
								target="_blank"
							>
								<Icon name="mdi:google" size={32} />
							</a>
							<a
								href="https://linkedin.com/in/hongyicai"
								target="_blank"
							>
								<Icon name="mdi:linkedin" size={32} />
							</a>
							<a href={resume} target="_blank">
								<Icon
									name="mdi:file-document-outline"
									size={32}
								/>
							</a>
						</div>
					</div>

					<div id="container-intro-text">
						<h2>Hongyi Cai | 蔡弘毅</h2>

						<p>
							Ni hao! I am a software engineer & team lead in
							infinity datatech, working with Java Springboot,
							Keycloak, PostgreSQL, Clickhouse, Kafka, Kubernetes
							Bare Metal and etc. Other than working life, I am
							also into Linux, Golang, Distributed Systems,
							Databases and Operating Systems. Always be frenzy
							about new tech things.
						</p>
						<p>
							As of academia, interested in AI research, e.g. <b
								>Video Understanding & Generating, Embodied AI &
								VLA, Multimodal, Data-centric AI, Natural
								Language Processing. I am currently looking for
								a PhD position or a funded RA position,
								available starting from 2026 September, physical
								internship preferred.
							</b> Kindly contact me if you have any opportunities:
							<a href="mailto:xcloudfance@gmail.com"
								>hongyicai's gmail</a
							>.
						</p>
						<p>
							Nothing is more interesting than seeing how things
							work. Digging into the details and prying off the
							diminutive innuendo that nobody cares would one day
							be useful in an unexpected way.
						</p>

						<p>
							I am also a freelance teacher in A-level Computer
							Science during my spare time and English during my
							summer holiday of 12th grade. I am going to finish
							my undergraduate degree of Computer Science at
							Universiti Malaya in 2027 Feb. You can view my full
							<a href={resume} target="_blank">resume here</a>.
						</p>
						<a href="https://github.com/xcloudfance/hongyicai-blog"
							>Blog Template</a
						>
					</div>
				</div>

				<div id="container-intro-research-list">
					<h4>Preprint / Publications</h4>
					<ul>
						{
							sortedPubs.map((pub) => (
								<li>
									<PublicationCard
										title={pub.title}
										abstract={pub.abstract}
										pubDate={pub.pubDate}
										heroImage={pub.heroImage}
										conference={pub.conference}
										href={pub.href}
										authors={pub.authors}
									/>
								</li>
							))
						}
					</ul>
				</div>

				<div id="container-experience">
					<h4>Education</h4>
					<ul>
						{
							experienceData.education.map((item) => (
								<li>
									<Experience
										time={item.time}
										organization={item.organization}
										title={item.title}
									/>
								</li>
							))
						}
					</ul>

					<h4>Work Experience</h4>
					<ul>
						{
							experienceData.work.map((item) => (
								<li>
									<WorkExperience
										time={item.time}
										organization={item.organization}
										title={item.title}
									/>
								</li>
							))
						}
					</ul>
				</div>
			</div>
		</main>
		<Footer />
	</body>
</html>
